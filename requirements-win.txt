# Windows-only extras: legacy .doc -> .docx conversion via Microsoft Word COM.
# Optional. The app runs fine without these; only the local CLI path
# `parse_pi_stream()` for legacy .doc files needs them.
# Install with:  pip install -r requirements-win.txt
doc2docx==0.2.4
pywin32==305
