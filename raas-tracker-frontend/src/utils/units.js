// Shared unit list for chemical stock (select options, mapping targets).
// Kept here (not in a component file) so react-refresh's
// only-export-components rule stays satisfied.
export const COMMON_UNITS = ['KG', 'G', 'L', 'ML', 'PCS', 'DRUM', 'BOTTLE', 'BOX'];
